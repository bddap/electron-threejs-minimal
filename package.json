{
  "name": "electron-three-minimal",
  "version": "0.0.0",
  "description": "Simple template.",
  "scripts": {
    "start": "electron app",
    "postinstall": "cd app && npm install && electron-rebuild -m node_modules --pre-gyp-fix"
  },
  "dependencies": {
    "electron": "^1.4.2",
    "electron-rebuild": "^1.2.1"
  }
}
